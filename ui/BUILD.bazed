const { nextJs, glob, env } = require("@nersent/bazed");

env("env", {
  file: ".env",
});

nextJs("ui", {
  srcs: glob("src/**/*.*"),
  deps: [
    "//common/js",
    "//common/ui",
    "//common/auth/ui",
    "//common/next",
    "//common",
    ":env",
  ],
  publicEnvs: ["API_URL"],
});

module.exports = global.rules;
