import React from "react";
import styled from "styled-components";

const StyledTest = styled.div``;

export const Test = React.forwardRef(() => {
  // return <StyledTest>1</StyledTest>;
  return (
    <div>
      2ko0awowtapo jrairjwaioroiwajoi
      <StyledTest />
    </div>
  );
});
